package messages

import (
	"errors"
)

var ERR_INVALID_MESSAGE = errors.New("Invalid message")
var ERR_INVALID_CATEGORY = errors.New("Invalid category")
var ERR_INVALID_KEYWORD = errors.New("Invalid keyword")
